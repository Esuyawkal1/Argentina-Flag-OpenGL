#ifndef FLAG_H
#define FLAG_H

// Core rendering callbacks
void display();
void reshape(int w, int h);

// Flag system helpers
void drawFlagBackground(float width, float height);
void drawSunWrapper(float width, float height);

#endif
